let fs = require('fs')
let dirs = fs.readdirSync(__dirname+'/node_modules')
let things = {}

for(var dir of dirs){
	if(dir.indexOf('ignore') < 0) {
		let thing = dir.replace(/.js/, '')
		things[thing] = require(`${dir}`)
	}
}

module.exports = things