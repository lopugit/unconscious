let fs = require('fs')
let modules = fs.readdirSync(__dirname+'/node_modules')
let things = {}

for(var module of modules){
	if(module.indexOf('ignore') < 0) {
		let thing = module.replace(/.js/, '')
		things[thing] = require(`${module}`)
	}
}

module.exports = things